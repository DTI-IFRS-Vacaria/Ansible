---
- name: Update web servers
  hosts: all
  become: true
  become_method: sudo
  become_user: semaphore

  tasks:
  - name: Creating an empty file
    file:
      path: "/teste"
      state: touch
  - name: Install apache httpd 
    ansible.builtin.apt:
      name: fail2ban
      state: present
    become: true
    become_method: sudo
    become_user: semaphore
